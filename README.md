# Project Name

A CLI Task Management App that allows users to Add, Delete, Update and view available tasks.

## Features

- Uses JSON file as a database to persist data
- Add, delete and Update tasks
- View available tasks based on their statuses
- Easy to configure and extend

## Technologies Used

- Java (Version X)
- File handling

## Prerequisites

Before running this project, make sure you have installed:

- Java JDK (version X or later)

Verify installation:

```bash
java -version
```

## Installation

1. Clone the repository:

```bash
git clone https://github.com/your-username/project-name.git
```

2. Navigate to the project directory:

```bash
cd project-name
```


## Running the Application

### Using Java

```bash
java -jar target/project-name.jar
```


## Usage

Describe how users can use your application.

- When you run the application, it would show a list of operations
- Pick the operation to run
- and follow the application instructions


## Link
- https://roadmap.sh/projects/task-tracker